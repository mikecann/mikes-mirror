{
  "name": "mikes-mirror",
  "version": "0.1.0",
  "private": true,
  "main": "electron-starter.js",
  "dependencies": {
    "electron": "^1.8.4",
    "python-shell": "^0.5.0"
  },
  "scripts": {
    "start": "tsc && concurrently -n tsc,electron \"tsc -w\" \"yarn start-electron\"",
    "start-electron": "electron dist/main.js"
  },
  "devDependencies": {
    "concurrently": "^3.5.1",
    "typescript": "^2.7.2"
  }
}
