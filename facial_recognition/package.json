{
  "name": "face_recognition",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "docker-compose build && docker-compose run face_recognition bash",
    "start": "docker-compose up --build"
  }
}
